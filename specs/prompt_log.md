#
`create 2 persona.md files I can use to review every stage of my project. One senior software engineer, one senior product design and any other persona that is important to consider`

`For this project I want to create a heavily gamified flashcard, spaced repetion/ memorisation assisstant . The name of the game is gami-fried :  Dont get cooked so there should be a cooking aspect to it. Help me to ideate some potential ideas and project breakdowns`

`Looking at this MVP. I want to change around the user flow. User should sign in and enter game and have the opportunity to enter what subject/topic they want to learn. Then an AI api will create relevant flash card decks 4-5 cards and load the game. Upon game load, the user will see the available 2-3 recipes (decks) at that point. Then they can open the shop (start a session) then customers will come in asking for a range of orders (combinations of card decks to go through) . In serviing each customer the user will be repeatedly working through the flashcard decks for each respective dish.  After about 2-3 customers the day ends and shop closes and a summary of results is visible. Then the user has the option to move on to the next day, and similar gameplay continues. Beyond the MVP there may be option to make the game progressively more difficult and other such thinngs.`

`create a list o github issues I can create to cover all of these tasks`

`what endpoints should I have in  the program.cs file `

`add the functionality to create a new deck (just a hard coded seed) . create the endpoint and connect to thte frontend ?`

`Get Create deck to run when I click the Generate new deck. Ensure generate deck logic is in a separate component`

`Enable user to to create a deck using AI, so I would imput a prompt/message of what the deck should be about and the output needs to be a structured JSON of a questions and answers, do you understand ??`

`How can I add functionality to edit decks, edit deck winodw would show the current prompt and current cards, then they can either hand edit the cards or the prompt and 'regenerate' questions`

` Enable to delete a question/answer from a deck`

`I want users to be able to take a deck and start a session of Anki style quizzing. On the Deck display page there is a button to start session and then go into the game space with the game play . Do you understand ? Implement this`

`improve all of the src frontend files`

`add a light mode and dark mode switcher `

# 